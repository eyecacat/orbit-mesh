[phases.setup]
nixPkgs = ["nodejs_18", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm i --no-frozen-lockfile"]

[phases.build]
cmds = ["node scripts/build.js"]

[start]
cmd = "node server/serve.js"
