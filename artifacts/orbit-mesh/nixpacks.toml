[phases.setup]
nixPkgs = ["nodejs_18", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm i --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/orbit-mesh exec node artifacts/orbit-mesh/scripts/build.js"]

[start]
cmd = "node artifacts/orbit-mesh/server/serve.js"
